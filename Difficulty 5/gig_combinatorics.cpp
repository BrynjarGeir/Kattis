#include <vector>
#include <iostream>
using namespace std;

int main() {
    vector<int> songs;
    int n, song;
    unsigned long long ans;

    cin >> n;

    for(int i = 0; i < n; i++) {
        cin >> song;
        songs.push_back(song);
    }


}