#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    int n, s;
    string number;
    cin >> n >> s;
    vector<int> cards;

    for(int i = 0; i < n; i++) {
        cin >> number;
        
        for()
        
    }
}