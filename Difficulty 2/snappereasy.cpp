#include <iostream>
using namespace std;

int main() {
    int T;
    cin >> T;

    for(int i = 1; i <= T; i++) {
        int N, K;
        cin >> N >> K;
        // The first one is for power and second one for on/off
        int max_power = 0;
        pair<bool, bool> snappers[N];
        snappers[0].first = true;
        for(int j = 0; j < K; j++) {
            
        }
    }
}