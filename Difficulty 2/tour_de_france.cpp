#include <iostream>
#include <iomanip>
#include <algorithm>
using namespace std;

int main() {
    int f, r;
    
    while(cin >> f && f != 0) {
        cin >> r;
        double front[f], rear[r];
        
        for(int i = 0; i < f; i++) {
            cin >> front[i];
        }
        for(int i = 0; i < r; i++) {
            cin >> rear[i];
        }

        double spread[f*r];

        for(int i = 0; i < f; i++) {
            
        }
    }
}