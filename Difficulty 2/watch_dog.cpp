#include <iostream>
#include <vector>
using namespace std;

int main() {
    int N, S, H, x, y;
    cin >> N;

    while(N--) {
        cin >> S >> H;
        vector<pair<int, int>> hatches;
        for(int i = 0; i < H; i++) {
            cin >> x >> y;
            hatches.push_back({x,y});
        }
        
        for(int )
    }
}