#include <iostream>
#include <set>
using namespace std;

int main() {
    int N, K, R, Di;
    cin >> N >> K >> R;
    int mdna[N];
    set<int> already;

    for(int i = 0; i < N; i++) cin >> mdna[i];
}