#include <iostream>
#include <vector>
#include <map>
using namespace std;

int main() {
    int n;
    cin >> n;
    string line;
    map<string, vector<string>> 
    cin.ignore();

    for(int i = 0; i < n; i++) {
        getline(cin, line);


    }
}