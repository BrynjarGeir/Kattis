#include <iostream>
#include <vector>
using namespace std;

vec

int main() {
    int h, w;
    cin >> h >> w;
    int grid[h][w];
    for(int i = 0; i < h; i++) {
        for(int j = 0; j < w; j++) {
            cin >> grid[i][j];
        }
    }

    vector<int> steps;

    for        

}