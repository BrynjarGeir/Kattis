#include <iostream>
#include <vector>
#include <tuple>
using namespace std;

int main() {
    int N, K, Q, T, M, S;
    cin >> N >> K;
    vector<tuple<int, int, 
    for(int i = 0; i < N; i++) {

    }
}