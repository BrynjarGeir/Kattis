#include <iostream>
using namespace std;

int main() {
    int R, C;
    cin >> R >> C;
    char grid[R][C];

    for(int i = 0; i < R; i++) {
        for(int j = 0; j < C; j++) {
            cin >> grid[i][j];
        }
    }

    for(int j = 0)
}