#include <iostream>
#include <set>
using namespace std;

int main() {
    int N, M, l, r;
    cin >> N >> M;

    //Maybe try to find all of the ones that are connected to 1
    //Somehow try to keep track of how I can get to house nr. 1
    //I Want to use list, is that something? Maybe simulate a list?
    //Maybe a map that has each connection and if I reach 1 then shrink the map?

}