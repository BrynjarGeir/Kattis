#include <iostream>
#include <math.h>
#include <string.h>
using namespace std;

string toDeci(char *str, int base) {
    int len = strlen(str);
    int power = 1;
}

int main() {
    string number;
    cin >> number;


}