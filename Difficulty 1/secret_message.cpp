#include <iostream>
#include <math.h>
using namespace std;

int main() {
    int N;
    string to_encrypt, encrypted;
    cin >> N;

    for(int i = 0; i < N; i++) {
        cin >> to_encrypt;
        encrypted = "";
        int M = pow(ceil(sqrt(to_encrypt.size())),2);
        int grid[M][M];
        for (int i = 0; i < M; i++) {
            for (int j = 0; j < M; j++) {
                to_encrypt.p
            }
        }
    }
}