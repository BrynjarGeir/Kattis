#include <iostream>
#include <map>
using namespace std;

int main() {
    int n, s_prev = -1, f_prev = -1, s, f, count = 0;
    cin >> n;

    while(n--) {
        if(s_prev == -1 && f_prev == -1) {
            cin >> s_prev >> f_prev;
        } else {
            cin >> s >> f;
        }

    }

    
}