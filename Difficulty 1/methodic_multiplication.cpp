#include <iostream>
using namespace std;

int main() {
    string x,y,left,right;
    cin >> x;
    cin >> y;

    if (x == "0" || y == "0") {
        cout << 0;
        return 0;
    }

    while(!x.empty()) {
        left = 
    }
}