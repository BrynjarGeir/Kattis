#include <iostream>
#include <vector>
using namespace std;

int main() {
    int C, N, num, prev, diff, org;
    string correct = "", incorrect = "";
    cin >> C >> N;

    for(int i = 0; i < N; i++) {
        cin >> num;
        
    }
}