#include <iostream>
#include <set>
using namespace std;

int main() {
    int N, M, K;
    cin >> N >> M >> K;
    char grid[N][M];

    for(int i = 0; i < N; i++) {
        for(int j = 0; j < M; j++) {
            cin >> grid[i][j];
        }
    }

    
}